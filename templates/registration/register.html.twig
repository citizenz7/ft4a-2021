{% extends 'base.html.twig' %}

{% block title %}{{ site_url}} : créer un compte{% endblock %}

{% block body %}
    {{ include('_flash.html.twig') }}

    <div class="row mt-3 d-flex justify-content-center mx-2">

        <div class="col-sm-4 small mb-2 py-3 pe-5" style="text-align:justify;">
            <h2>Créer un compte</h2>
            Vous allez créer un compte sur ft4a.fr.
            <br>
            Le fait de devenir membre vous fera bénéficier de plusieurs avantages :
            <ul>
                <li>pouvoir downloader (télécharger) des torrents</li>
                <li>pouvoir uploader (proposer) des torrents,</li>
                <li>disposer de statistiques personnelles</li></li>
                <li>disposer d'un espace membre et d'une messagerie interne,
                <li>etc.</li>
            </ul>
            Merci de choisir un pseudo, un mot de passe et une adresse e-mail. Vous recevrez un e-mail de notre part avec un lien qui vous permettra d'activer votre nouveau compte.
            (Eventuellement, merci de vérifier votre répertoire Spam)
        </div>

        <div class="col-sm-4 py-3 mb-2 border-start ps-5">

            {{ form_start(registrationForm) }}
                {{ form_row(registrationForm.username) }}
                {{ form_row(registrationForm.email) }}
                {{ form_row(registrationForm.plainPassword.first) }}
                {{ form_row(registrationForm.plainPassword.second) }}
                <div class="text-center pt-2">
                    <a href="{{ path('about') }}" class="text-decoration-none"><i class="fas fa-angle-right"></i> Lire les C.G.U. <i class="fas fa-angle-left"></i></a>
                    {{ form_row(registrationForm.agreeTerms) }}
                </div>

                <div class="mt-3 d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Créer un compte</button>
                    <button type="reset" class="btn btn-secondary">Annuler</button>
                </div>
            {{ form_end(registrationForm) }}
        </div>
    </div>

{% endblock %}
