{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="mt-3">
    <button class="btn btn-sm btn-success">{{ button_label|default('Save') }}</button>
    <button class="btn btn-sm btn-secondary" type="reset">Reset</button>
    <a class="btn btn-sm btn-primary" href="{{ path('categories_index') }}">Back to list</a>
</div>
{{ form_end(form) }}
