{% extends 'base.html.twig' %}

{% block title %}Demander un nouveau mot de passe{% endblock %}

{% block body %}
    <h1>Demander un nouveau mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Nouveau mot de passe</button>
    {{ form_end(resetForm) }}
{% endblock %}
