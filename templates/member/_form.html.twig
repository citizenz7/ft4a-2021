{{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.pid) }}
    {{ form_row(form.avatar_file) }}
    {{ form_row(form.signature) }}

    <div class="mt-3 d-flex justify-content-between">
        <button type="reset" class="btn btn-secondary py-2 px-3">Annuler</button>
        <button class="btn btn-success py-2 px-3" type="submit">{{ button_label|default('Modifier') }}</button>
    </div>
{{ form_end(form) }}
